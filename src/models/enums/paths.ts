const enum Paths {
  "POSTS" = "/posts",
  "POST" = "/posts/:postId",
  "WRITE_POST" = "/posts/new",
  "EDIT_POST" = "/posts/:postId/edit",
  "MY_POSTS" = "/posts/my-posts",
  "LOGIN" = "/login",
  "SIGN_UP" = "/sign-up",
}

export default Paths;
